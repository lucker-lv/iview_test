const state = {
  value: ''
}

const getters = {

}

const actions = {}

const mutations = {
  updateValue (state, value) {
    state.value = value
  }
}

export default {
  state,
  getters,
  actions,
  mutations
}
